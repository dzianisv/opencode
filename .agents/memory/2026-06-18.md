
## Workflow Bare-Globals Shim — E2E Proven

- PR #232: `feat/workflow-bare-globals` branch from dev
- Cherry-picked 4 commits: engine base, scan fix, HTTP test, bare-globals shim
- **E2E verified**: server started from source (`bun run src/index.ts serve`), `/workflow hello` executed successfully via HTTP API
- Discovery works for `.opencode/workflows/` and `.agents/workflows/`
- `agent()` calls route to `ctx.agent()` correctly but hit pre-existing `schema._zod.def` crash in tool registration (same bug as `repro-crash` todo)
- 4/4 tests pass on clean branch
- Path to run from source: `cd packages/opencode && bun run src/index.ts serve --hostname 127.0.0.1 --port 4096`
- API: POST `/session/:id/command` with `{"command": "workflow", "arguments": "<name>"}` for sync workflow execution
